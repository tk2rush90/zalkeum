import { CarouselContentDirective } from './carousel-content.directive';

describe('CarouselContentDirective', () => {
  it('should create an instance', () => {
    const directive = new CarouselContentDirective();
    expect(directive).toBeTruthy();
  });
});
